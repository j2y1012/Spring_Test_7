<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="MemberMapper">
 
  <insert id="memberJoin">
  insert into member values(#{id},#{pw},#{name},#{email},#{phone},#{age},#{job},#{fname},#{oname})
  </insert>
  
  <select id="memberLogin" resultType="com.iu.member.MemberDTO" parameterType="com.iu.member.MemberDTO">
  select * from member where id=#{id} and pw=#{pw}
  </select>
  
  
 
  <update id="memberUpdate" >
  update member set name=#{name},fname=#{fname} oname=#{oname}
 </update>
  
  
  <delete id="memberDelete" >
  delete member where id=#{id}
  </delete>
  
  
  
  
  
  </mapper>